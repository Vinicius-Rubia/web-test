export enum EClickTypes {
  LEFT = "left",
  RIGHT = "right",
  DOUBLE = "double",
}

export enum ESelectorTypes {
  ID = "id",
  NAME = "name",
  CSS = "css",
  XPATH = "xpath",
  LINK_TEXT = "link-text",
  TAG = "tag",
}